import math

x = float(input('Bведіть х:'))
y = float(input('Bведіть y:'))
z = float(input('Bведіть z:'))

print(x,y,z,)

a = (math.sqrt(math.fabs(x - 1)) - math.sqrt(math.fabs(y)))/(1+x^2/2+y^2/4)
b = (x**3)*(math.atan^3(z) + math.e)

print('a=', a, 'b=', b)